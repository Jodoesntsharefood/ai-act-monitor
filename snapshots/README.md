Snapshots directory
This folder stores JSON snapshots of the standards page state.
The first run will create `standards_snapshot.json` as a baseline.
Subsequent runs compare against it to detect changes.
